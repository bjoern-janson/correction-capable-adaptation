# ASI-0 — Results

## Terminal primary result

\[
\boxed{C=0,\qquad A=0,\qquad \mathrm{STOP}}
\]

Canonical observation:

```text
n_targets                         16
mean base score                  0.20833333333333331
mean aligned score               0.20833333333333331
mean misaligned score            0.20833333333333331
mean random-edit score           0.20833333333333331
aligned valid selection          14/16
misaligned valid selection       14/16
aligned gate admissions           0/16
misaligned gate admissions        0/16

C                                  0
A                                  0
L_C                                0
L_A                                0
PRIMARY                            STOP
REPLICATION                        NOT AUTHORIZED
ASI-0 GREEN                        FALSE
```

The primary alternative

\[
C>0\land A>0
\]

was not established.

## Execution note

The first canonical attempt produced no scientific outcome because the original four-point Boolean domain could not satisfy the frozen global uniqueness requirement for development and concealed prompts. That attempt remains **UNOBSERVED**, not negative.

The input-domain layer was repaired prospectively before outcomes:

```text
boolean_pair -> boolean_triple
```

The repaired execution path passed a frozen CPU↔GPU behavioral-equivalence diagnostic before the first outcome-bearing canonical run.

## Why all primary outcome means were equal

The protected acceptance policy admitted no aligned or misaligned selected patch:

```text
aligned admissions      0/16
misaligned admissions   0/16
```

Under the prospectively frozen policy-level intent-to-treat object, rejected modifications became no-op/base behavior. Therefore the realized primary arms collapsed to the base outcome.

This is a property of the frozen intervention policy, not an estimator repair.

## Earned claim

> **Under the frozen proposal + protected-acceptance policy, correctly assigned development evidence produced no concealed capability gain and no incremental outcome leverage relative to misaligned assignment.**

## Post-outcome mechanism diagnosis

The outcome-blind diagnosis used candidate identity and the frozen protected-regression cache rather than concealed performance to classify mechanisms.

Two bottlenecks were visible:

### 1. Inference bottleneck

Aligned and misaligned parsed selection states were identical on 14 of 16 targets. Conditional on a valid parse, selection collapsed to stereotyped candidates within each task family.

The evidence therefore had weak control over candidate identity in the warranted direction.

### 2. Modification / isolation bottleneck

Across the complete frozen pool:

```text
candidate patches                      16
passing protected gate                  1
failing baseline preservation          15/16
```

Across the realized aligned/misaligned arms:

```text
valid selected patches                 28
rejected selected patches              28/28
```

Every valid selected patch newly failed the exact-token `PINE` protected probe relative to base.

The protected gate appears to have functioned as designed: it detected interference and prevented deployment.

## Scientific consequence

ASI-0 did not justify relaxing the gate or trying a stronger model inside the same experiment. Instead it motivated a new prospective decomposition:

\[
E\rightarrow C_{\mathrm{selected}}\rightarrow M_{\mathrm{effective}}\rightarrow(Y_T,Y_P),
\]

with selection and modification mechanisms identified separately.
