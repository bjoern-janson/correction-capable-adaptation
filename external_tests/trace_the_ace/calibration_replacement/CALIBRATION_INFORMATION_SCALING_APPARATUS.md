# Information-scaling apparatus equivalence

## Status

**EXECUTION-MECHANICS REPAIR — NO SCALING DECISION RESULT OBSERVED BEFORE REPAIR**

The first full PR #44 execution reproduced the PR #43 `m=1` apparatus exactly and then hit a runtime wall while materializing prospectively seeded synthetic session-noise blocks. No `m>1` decision cell, information-response point, or scaling gate had been computed.

The scientific identity remains unchanged:

```text
outcome seeds       SeedSequence([1831, sentinel, ratio, replicate, cohort])
truth geometry      unchanged
scale signs         unchanged
copula rho          0.15
row outcomes        unchanged
session aggregation unchanged
stored dtype        float64
bootstrap           unchanged
```

Only process scheduling changed: the original serial cohort materializer was replaced by a 5-process forked materializer writing non-overlapping cohort blocks.

## Bit-for-bit equivalence proof

A complete `coarse / R=0` 16-cohort noise artifact from the interrupted serial execution was preserved and compared with a fresh artifact generated by the parallel apparatus.

```text
serial artifact SHA-256
7d4ff4c435a6eb7330fcfcc5b8e0c08e43397190540122ad4d57c3082d96e16f

parallel artifact SHA-256
7d4ff4c435a6eb7330fcfcc5b8e0c08e43397190540122ad4d57c3082d96e16f
```

The artifacts are bit-for-bit identical.

Therefore the repair changes execution mechanics only and does not change any scientific quantity, RNG stream, fixture, threshold, bootstrap rule, or decision criterion.
