# D_main objective-main-effect suppression diagnostic result

## Status

**CLOSED — LOCAL SUPPORT FOR `D_main` — BROAD `H_O` REMAINS OPEN**

The prospectively frozen four-arm diagnostic has now executed completely under the validated exact materialized-cell apparatus successor.

## Apparatus resolution

The scientific object did not change. Profiling showed that the exact B0/fold-0 SGD fit itself completes in about 25.76 seconds at `n_iter=84`; the prior block was localized to the monolithic execution harness/runtime lifecycle.

The two-process materialize/fit successor reproduced the already existing exact B0/fold-0 prediction cell bitwise:

```text
row indices        identical
n_iter             84 == 84
probabilities      bitwise identical
max abs diff       0.0
NPZ SHA-256        880273c43169b17344fb77db884b92144512b66867042ba5c9fdce83ce37ebec
```

All 20 frozen arm/fold fits subsequently completed and converged before `max_iter=500`.

## Frozen quantities

```text
Delta0 = LL(C0) - LL(B0)
Delta1 = LL(C1) - LL(B1)
Psi    = Delta0 - Delta1
```

with:

```text
B0 = [Z_T, X_ordinary, Z_S]
C0 = B0 + R_centered
B1 = [Z_T, Z_O, X_ordinary, Z_S]
C1 = B1 + R_centered
```

## Result

Pooled raw-sigmoid log loss:

```text
B0  0.5772381267
C0  0.5768994174
B1  0.5570723486
C1  0.5571439724
```

Without explicit objective identity:

\[
\Delta_0=-0.0003387093
\]

with paired session-cluster 95% CI:

\[
[-0.0005356914,-0.0001488892].
\]

With explicit objective identity restored:

\[
\Delta_1=+0.0000716238
\]

with 95% CI:

\[
[-0.0001432647,+0.0002675572].
\]

The prospective suppression discriminator is:

\[
\Psi=\Delta_0-\Delta_1=-0.0004103331
\]

with 95% CI:

\[
[-0.0006473098,-0.0001835602].
\]

Both required `D_main` support gates pass:

```text
Delta0 point < 0                 PASS
Delta0 CI upper < 0              PASS
Psi point < 0                    PASS
Psi CI upper < 0                 PASS
```

Therefore:

```text
D_main local diagnostic: SUPPORT
```

## Diagnosis

Under this frozen probe, the centered historical semantic-conditioning residual carries stable incremental predictive information when explicit objective identity is absent. Its incremental value is not established once objective identity is explicitly restored, and the change in incremental value is stably negative.

This supports the local explanation that objective information absorbs/suppresses predictive structure that the same residual can otherwise carry.

It does **not** establish that all objective-conditioned interpretation is globally redundant, nor does it refute broad `H_O`. `D_temporal`, `D_granularity`, `D_identifiability`, and `D_redundancy` remain unresolved as broader alternatives beyond the scope of this discriminator.

## Authority ceiling

Gained:

- local diagnostic support for `D_main`;
- closure of the current conditioning prerequisite for constitution of the future mature non-CCA baseline.

Not gained:

- global `H_O` support or refutation;
- authorization of a new conditioning operator or `M2-III`;
- calibration resolution;
- `Z_E / Z_D / Z_C / Z_P` authorization;
- CCA, causal, G1, PMC, JT, or `C_improve` authority.

Generated prediction rows remain local and uncommitted.

Local artifact identities:

```text
20-cell manifest SHA-256  fe7ce254f6da4c8975a472a20c217f568fa49b8d96ede915af5d6585a2722f5a
OOF SHA-256               532c7f44fcab91f9c123a7dc4f83c8d326bdb816f3dec7759a430bd6bfb72ce9
aggregate record SHA-256  b2a678064a8d8052314896421cb17eb06793d641555abad09e21c85e76dbc92c
```
